/*
 * Simple runtime-only debug flags. Only respected when DeviceCompat.isDebug() is true.
 */
package com.shatteredpixel.shatteredpixeldungeon.mechanics;

public class DebugConfig {
    public static boolean godMode = false;
}
